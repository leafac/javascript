<!--
maybe move browser.js into index.ts? (Similar to what happens in @leafac/css?)

If enabling live-navigation, don’t add ‘onload’ to <body> (or the browser will run it, besides we running it, but then it won’t rerun it on subsequent loads)
-->